export default function InstagramSchedulePage() {
  return (
    <div className="space-y-6">
      <header className="rise">
        <h1 className="text-2xl font-semibold">Schedule</h1>
        <p className="mt-1 text-sm text-[color:var(--muted)]">
          Instagram posting schedule (separate from YouTube). Coming with the Reels
          worker pipeline.
        </p>
      </header>
      <div className="panel rise p-6 text-sm text-[color:var(--muted)]">
        Prepare Avatar + Training now. Schedule UI will unlock when Meta publish is
        connected.
      </div>
    </div>
  );
}
