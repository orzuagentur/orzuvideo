export default function InstagramCostsPage() {
  return (
    <div className="space-y-6">
      <header className="rise">
        <h1 className="text-2xl font-semibold">Costs</h1>
        <p className="mt-1 text-sm text-[color:var(--muted)]">
          HeyGen + ElevenLabs + OpenAI spend for Instagram Reels will show here.
        </p>
      </header>
      <div className="panel rise p-6 text-sm text-[color:var(--muted)]">
        No Instagram usage events yet.
      </div>
    </div>
  );
}
